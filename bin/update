#!/usr/bin/env ruby
# frozen_string_literal: true

# Exit cleanly from an early interrupt
Signal.trap("INT") do
  puts "\n#{caller.join("\n")}"
  exit 1
end

require_relative "./cli"

CLI.update(ARGV)
